// DEfine a function hide that takes in an argument, representing a hiden location. This function should store the location in a local variable hidelocation.
function hide(a) {
    function seek() {
        const hideLocation = a;
        return hideLocation;

    }
}
const playNow = hide("Behind the door");
console.log(playNow());